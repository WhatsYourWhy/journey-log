node_modules/
playwright-report/
test-results/
.claude/

# Build manifest is generated by scripts/build-content.js; the JSON content
# files alongside it are committed because they are also injected into
# app/index.html and should stay in sync.
app/data/manifest.json
